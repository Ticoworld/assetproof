"use client";

/**
 * FastHUD — Phase 2 stub
 *
 * Will display fast on-chain BNB Chain data (contract verification,
 * holder distribution, liquidity metrics) once the data pipeline is
 * implemented in Phase 2.
 */
export function FastHUD() {
  return (
    <div className="rounded-lg border border-zinc-800 p-4 text-zinc-600 font-mono text-xs">
      Fast analysis panel — Phase 2 pending
    </div>
  );
}
