"use client";

/**
 * SlowVision — Phase 2 stub
 *
 * Will display the full AI attestation verdict (disclosure analysis,
 * visual evidence, risk findings) once implemented in Phase 2.
 */
export function SlowVision() {
  return (
    <div className="rounded-lg border border-zinc-800 p-4 text-zinc-600 font-mono text-xs">
      AI analysis panel — Phase 2 pending
    </div>
  );
}
